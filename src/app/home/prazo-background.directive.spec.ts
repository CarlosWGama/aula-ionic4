import { PrazoBackgroundDirective } from './prazo-background.directive';

describe('PrazoBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new PrazoBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
